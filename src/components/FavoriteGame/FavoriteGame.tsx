

export const FavoriteGame = () => {
  return (
    <div>
      ddd
    </div>
    // <section>
    //   {favorite.length} saved {favorite.length === 1 ? 'game' : 'games'}
    //   {favorite.length ? (favorite.map((game, index) => (
    //     <div className="flex w-full gap-2">
    //       <div key={index}>
    //         {game.fixture.status.elapsed === 90 ? (<div>{game.fixture.status.elapsed}+{game.fixture.status.extra}'</div>) : game.fixture.status.elapsed}
    //       </div>
    //       <div className="flex justify-center">
    //         {game.teams.home.name} {game.goals.home} - {game.goals.away} {game.teams.away.name}
    //       </div>
    //     </div>
    //   ))) : null}
    // </section>
  )
}